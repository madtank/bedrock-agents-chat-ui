# Environment variables for local development
# Copy this file to .env.local and fill in your values

# Cognito Configuration
# Note: The VITE_ prefix is required for Vite to expose these variables to the frontend
VITE_COGNITO_REGION=us-east-1
VITE_COGNITO_USER_POOL_ID=
VITE_COGNITO_USER_POOL_CLIENT_ID=
VITE_COGNITO_IDENTITY_POOL_ID=

# Bedrock Configuration
VITE_BEDROCK_REGION=us-west-2
VITE_BEDROCK_AGENT_ID=
VITE_BEDROCK_AGENT_ALIAS_ID=
VITE_BEDROCK_AGENT_NAME=

# Note: For GitHub Actions CI/CD, add these variables as secrets without the VITE_ prefix
# The workflow will automatically map them to the correct environment variables